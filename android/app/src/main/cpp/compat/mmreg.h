#pragma once
// The multimedia registry header. Nothing in the tree names a symbol that is
// unique to it -- the wave format structure and its tags come from
// <mmsystem.h> -- so this exists to satisfy the include and nothing else.
#include "mmsystem.h"
